package july.ex_21072024forloop;

public class lab120functiondomoreexpm {

    public static void main(String[] args) {
        // Block of code which we can reuse.
        // Pre-built  ex Math.max()
        // User created / defined
        // two component of the function
        // 1] define
        // 2] call the function

       int result = Math.max(5,20);
       int result2 =Math.max(10,11);
        System.out.println(result);
        System.out.println(result2);
        }
    }

